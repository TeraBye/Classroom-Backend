server:
  port: 8086
  servlet:
    context-path: /api/questions

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/question_service"
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  services:
    classrooms: http://localhost:8084/api/classrooms
    exam: http://localhost:8088/api/exam
server:
  port: 8086
  servlet:
    context-path: /api/questions

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/question_service"
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

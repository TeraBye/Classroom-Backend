server:
  port: 8091
  servlet:
    context-path: /api/logs

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/logging_service"
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


server:
  port: 8086
  servlet:
    context-path: /api/post

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/post_service"
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  services:
    profile: http://localhost:8081/api/profile
    assignment: http://localhost:8083/api/assignments
